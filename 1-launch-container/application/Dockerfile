FROM node:18

WORKDIR /app

ENV ENVIRONMENT ${ENVIRONMENT}
ENV DB_USER ${DB_USER}
ENV DB_NAME ${DB_NAME}
ENV DB_HOST ${DB_HOST}
ENV DB_PORT ${DB_PORT}
ENV DB_PASSWORD ${DB_PASSWORD}

COPY package*.json .

RUN npm install

COPY . .

RUN npm run build

EXPOSE ${PORT}

CMD [ "npm", "start" ]